<nav>
  <h1><%= link_to 'Test Guru', tests_path %></h1>

  <% if logged_in? %>
    <p class='nav user'>Welcome, <b><%= current_user.email %> </b> Guru</p>
    <p class='nav user'><%= link_to 'Exit', session_path(session) , method: :delete %></p>
  <% else%>
    <p class='nav user'><%= link_to 'Register', signup_path %> / <%= link_to 'Login', login_path %></p>
  <% end %>

</nav>
