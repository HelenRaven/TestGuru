<h2 class="h2"><%= t('.gists')%></h2>

<table class="table text-center">
  <thead class="thead-light">
    <tr>
      <th scope="col"><%= t('.table.question') %></th>
      <th scope="col"><%= t('.table.url') %></th>
      <th scope="col"><%= t('.table.user') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @gists %>
  </tbody>
</table>
